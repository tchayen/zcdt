{
  "version": "0.2.0",
  "configurations": [
    {
      "preLaunchTask": "build",
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/zig-out/bin/exe",
      "cwd": "${workspaceFolder}"
    }
  ]
}
